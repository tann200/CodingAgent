This directory contains tests moved from the main test suite because they matched the "low value / deprecated / flaky / xfail / skip" heuristics.

They were moved so the main test runs become more stable. If you want any file restored, move it back from this directory.

Do not edit these files in-place; if you need to re-enable a test, restore it to its original path.
