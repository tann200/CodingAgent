#!/usr/bin/env bash
# DEPRECATED: use `scripts/run.sh --tui` or `uv run codingagent --tui` instead.
# start_tui.sh originally launched the Textual TUI directly.
# Now the TUI is the default mode for start.sh and scripts/run.sh.
