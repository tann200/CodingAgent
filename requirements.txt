# Runtime dependencies for CodingAgent (pinned)
requests==2.31.0
httpx==0.28.0
textual==8.1.0
openai==2.0.0
python-dotenv==1.0.0
typing-extensions==4.5.0
uv==0.10.0
PyYAML==6.0

# Development/test tools
pytest==7.4.0
ruff==0.12.0
black==23.9.0
mypy==0.991

# Create virtualenv with
# python -m venv .venv
# and activate with
# source .venv/bin/activate
