# Runtime dependencies for CodingAgent
# Requires Python >= 3.11
requests==2.31.0
httpx==0.28.0
textual==8.1.0
openai==2.0.0
python-dotenv==1.0.0
typing-extensions>=4.11.0
uv==0.10.0
PyYAML==6.0
langgraph>=0.2.0
langchain-core>=0.3.0
pydantic>=2.0.0
pandas>=2.0.0
lancedb>=0.10.0

# Development/test tools
pytest>=8.0.0
pytest-asyncio>=0.21.0
ruff==0.12.0
black==23.9.0
mypy>=1.0.0
tomli>=2.0.0

# Optional: OpenTelemetry tracing (Gap 8)
# Enable by setting OTEL_EXPORTER_OTLP_ENDPOINT env var.
# Install the exporter that matches your backend:
#   gRPC (port 4317, e.g. Grafana Tempo / Jaeger):
#     pip install opentelemetry-sdk opentelemetry-exporter-otlp-proto-grpc
#   HTTP (port 4318 / Honeycomb / OTLP/HTTP backends):
#     pip install opentelemetry-sdk opentelemetry-exporter-otlp-proto-http
# opentelemetry-sdk>=1.20.0
# opentelemetry-exporter-otlp-proto-grpc>=1.20.0
# opentelemetry-exporter-otlp-proto-http>=1.20.0

# Create virtualenv with
# python -m venv .venv
# and activate with
# source .venv/bin/activate
