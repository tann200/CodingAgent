# Runtime dependencies for CodingAgent
requests>=2.31.0
httpx>=0.28.0
textual>=8.1.0
openai>=2.0.0
python-dotenv>=1.0.0
typing-extensions>=4.5.0
uv>=0.10.0
PyYAML>=6.0

# Optional (but commonly used) - include for adapters and tooling

# Development/test tools (kept here for convenience; also present in pyproject.optional-dependencies)
pytest>=7.0.0
ruff>=0.12.0
black>=23.9.0
mypy>=0.991
