/*
TUI styles using .tcss files (Textual CSS). Keep styles in separate files,
one per component: main.tcss, sidebar.tcss, log_panel.tcss, provider_panel.tcss

These are placeholders so the repository contains the styles folder. Real
styles will be added when the Textual app is implemented.
*/

